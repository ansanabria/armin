# README assets

Drop launch visuals here, then uncomment the image lines near the top of the
root [`README.md`](../../README.md).

Suggested captures:

- `review.png` — a review session (front/back, rating buttons).
- `graph.png` — the prerequisite graph canvas with a few connected flashcards.

Optionally a single `demo.gif` showing the review → unlock flow instead.

Keep images reasonably sized (ideally < 1 MB each) so the README loads fast.
